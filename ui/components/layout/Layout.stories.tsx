import React from "react";
import { Layout } from "./Layout";

export default {
  title: "mc-build/Layout",
  component: Layout,
};
export const LayoutStory = (props) => <Layout {...props}></Layout>;
