import React from "react";
import { Header } from "./Header";

export default {
  title: "mc-build/Header",
  component: Header,
};
export const HeaderStory = (props) => <Header {...props}></Header>;
