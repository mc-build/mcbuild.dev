import React from "react";
import { ThemeProvider } from "./ThemeProvider";

export default {
  title: "mc-build/ThemeProvider",
  component: ThemeProvider,
};
export const ThemeProviderStory = (props) => <ThemeProvider {...props}></ThemeProvider>;
